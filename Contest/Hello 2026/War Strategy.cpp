#include <iostream>
#include <vector>
using std::cout;
using std::endl;

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    int p;
    std::cin >> p;
    for (int i = 0; i < p; i++) {
        std::vector<int> arr(3);
        for (int i = 0; i < 3; i++) std::cin >> arr[i];
        long long counter = 0;
        while (arr[2]--) {

        }
    }
    return 0;
}